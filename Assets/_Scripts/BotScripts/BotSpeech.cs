using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotSpeech : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /// <summary>
    /// finds if there's a fellow bot near by or not
    /// </summary>
    /// <returns></returns>
    private bool IsBuddyNearBy(){
        return false;
    }
    /// <summary>
    /// Finds if the buddy is In Dialogue Already
    /// </summary>
    private bool IsBuddyInDialogue(){
        return false;
    }
    /// <summary>
    /// Starts Dialogue Sequence with a fellow bot
    /// </summary>
    private void StartDialogue(){

    }
}
